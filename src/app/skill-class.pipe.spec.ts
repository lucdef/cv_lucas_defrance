import { SkillClassPipe } from './skill-class.pipe';

describe('SkillClassPipe', () => {
  it('create an instance', () => {
    const pipe = new SkillClassPipe();
    expect(pipe).toBeTruthy();
  });
});
